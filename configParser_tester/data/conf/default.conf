timeout = 60
max_clients = 100
buff_size = 9000
max_events = 100
backlog = 10

[server] # TODO VF CGI location

port = 8081;
	server_name = www.example.com example.com # list
	body_size = 9000
	# error_page 404 = error/404.html #map errorcode/path TODO VF check file, if code not defined use template
	#</> # location class
		# redirect = yourmom/ #checks only in root the redirection does not replace it
	#	methods = GET POST # list
	#	autoindex = on
		# index = index.html
	#	cgi = .py # list
	# <\/>
	# <test>
	# <\test>
	</lol/>
		autoindex = on #in case of 
 		methods = GET
		index = /cgi.html
		cgi = .py
		redirect = /cgi
	<\/lol/>
	</upload>
		redirect = storage
		methods = DELETE GET POST
	<\upload>
[\server]

[server]
	body_size = 2000
	</upload>
		redirect = storage
		methods = DELETE
	<\upload>
	port = 8080;
[\server]


[server]
body_size = 3000
port = 8082;
[\server]